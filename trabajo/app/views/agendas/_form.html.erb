<%= form_with(model: agenda, local: true) do |form| %>
  <% if agenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agenda.errors.count, "error") %> prohibited this agenda from being saved:</h2>

      <ul>
      <% agenda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hora %>
    <%= form.datetime_select :hora, id: :agenda_hora %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado, id: :agenda_estado %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :agenda_descripcion %>
  </div>

  <div class="field">
    <%= form.label :confirmacion %>
    <%= form.check_box :confirmacion, id: :agenda_confirmacion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
