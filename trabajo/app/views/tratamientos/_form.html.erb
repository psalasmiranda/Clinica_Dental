<%= form_with(model: tratamiento, local: true) do |form| %>
  <% if tratamiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tratamiento.errors.count, "error") %> prohibited this tratamiento from being saved:</h2>

      <ul>
      <% tratamiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tratamiento_nombre %>
  </div>

  <div class="field">
    <%= form.label :costo %>
    <%= form.number_field :costo, id: :tratamiento_costo %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :tratamiento_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
