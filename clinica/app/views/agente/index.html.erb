<table class="table">
<div class="list-group" >
  <a class="list-group-item active" >
    <font FACE= "roman" size= 12 align= center>Listado de Proveedores</font>
  </a>

<tr>
  <td align= center>Nombre</td>
  <td align= center>Apellido</td>
  <td align= center>Rut</td>
  <td align= center>Correo</td>
  <td align= center>Telefono</td>
  <td align= center>Direccion</td>

</tr>

    <% @agentes.each do |a| %>
        <tr>
          <td align= center><%= a.nombre %></td>
          <td align= center><%= a.apellidos%></td>
          <td align= center><%= a.rut%></td>
          <td align= center><%= a.correo %></td>
          <td align= center><%= a.telefono %></td>
          <td align= center><%= a.dirrecion %></td>
          <td><%= button_to 'Mostrar' ,agente_url(a), :class => "btn btn-primary" %></td>
          <td> <%= button_to 'Editar', editar_agente_url(a), :class => "btn btn-warning" %></td>
          <td><%= button_to 'Eliminar', a, :class => "btn btn-danger", method: :delete, data: {confirm: 'Desea Continuar'} %></td>

        </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @agentes %>
