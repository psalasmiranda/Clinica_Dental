<table class="table">
<div class="list-group" >
  <a class="list-group-item active" >
    <font FACE= "roman" size= 12 align= center>Listado de Tratamientos</font>
  </a>

<tr>
  <td align= center>Nombre Tratamiento </td>
  <td align= center>Descripcion</td>

</tr>

    <% @tratamientos.each do |a| %>
        <tr>
          <td align= center><%= a.nombre%></td>
          <td align= center><%= a.descripcion %></td>
          <td><%= button_to 'Mostrar' , tratamiento_url(a), :class => "btn btn-primary" %></td>
          <td> <%= button_to 'Editar', editar_tratamiento_url(a), :class => "btn btn-warning" %></td>
          <td><%= button_to 'Eliminar', a, :class => "btn btn-danger", method: :delete, data: {confirm: 'Desea Continuar'} %></td>

        </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @tratamientos %>
